import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    menu: {
        padding: 0,
    },
    btn: {
        width: '100%',
        padding: '10px 25px',
    },
});
