import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    cardContainer: {
        padding: '40px 20px',
        borderRadius: 15,
    },
    logoContainer: {
        height: 45,
        width: 45,
    },
    cardTeam: {
        paddingTop: 120,
    },
}));
