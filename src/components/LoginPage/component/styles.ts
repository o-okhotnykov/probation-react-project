import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    media: {
        margin: '10px auto',
        height: '30vh',
        width: '30vh',
    },
}));
