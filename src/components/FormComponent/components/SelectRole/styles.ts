import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    select: {
        marginTop: '0',
        padding: '0px',
        width: '80%',
        '& div': {
            padding: '10.5px 14px',
        },
    },
});
