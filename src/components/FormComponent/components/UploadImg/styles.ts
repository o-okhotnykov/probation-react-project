import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    userImg: {
        width: 100,
        height: 100,
        marginBottom: 25,
        borderRadius: '50%',
    },
});
